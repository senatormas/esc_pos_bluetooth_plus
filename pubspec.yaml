name: bluetooth_esc_printer
description: Modern Flutter plugin for ESC/POS Bluetooth printing (mobile only).
version: 1.0.0
homepage: https://github.com/senatormas/bluetooth_esc_printer
publish_to: none # remove if publishing to pub.dev

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.0
  esc_pos_utils_plus: ^2.0.1+6


dev_dependencies:
  flutter_test:
    sdk: flutter
